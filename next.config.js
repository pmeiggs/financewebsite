// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // next 15 requires this to be explicitly set for the pages router
  experimental: {},
};

module.exports = nextConfig;
